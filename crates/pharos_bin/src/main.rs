fn main() {
    // Sprint 1 placeholder. Sprint 4 wires this to pharos_core +
    // pharos_render for headless YAML-scene rendering.
    eprintln!("pharos-headless: Sprint 4 lands the runtime. Try again after Sprint 4.");
    std::process::exit(2);
}
